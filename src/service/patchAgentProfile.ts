import PatchAgentProfileOptions from '../serviceFactory/options/PatchAgentProfileOptions';
import Config from './Config';

export default (_config: Config) => {
  return async (_opts: PatchAgentProfileOptions): Promise<void> => {
    return;
  };
};
