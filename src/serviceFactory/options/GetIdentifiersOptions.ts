import GetOptions, { applyDefaultOptions as applyDefaultOptions2 } from '../utils/GetOptions';

type GetIdentifiersOptions = GetOptions;

export const applyDefaultOptions = applyDefaultOptions2;

export default GetIdentifiersOptions;
